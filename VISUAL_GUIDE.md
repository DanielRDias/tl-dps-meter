# DPS Meter Sharing Feature - Visual Guide

## Overview Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                    DPS Meter Sharing System                      │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────┐         ┌──────────────────────────────┐
│   Frontend (React)       │         │   Backend (Express.js)       │
│                          │         │                              │
│ ┌───────────────────┐    │         │ ┌──────────────────────────┐ │
│ │ DPS Meter         │    │         │ │ API Server               │ │
│ │ - Upload logs     │    │ HTTP    │ │ ┌────────────────────┐   │ │
│ │ - View stats      │◄──────────►  │ │ POST /api/share     │   │ │
│ │ - Share button    │    │ JSON    │ │ GET /api/share/:id  │   │ │
│ └───────────────────┘    │         │ │ GET /health         │   │ │
│                          │         │ └────────────────────┘   │ │
│ ┌───────────────────┐    │         │ ┌──────────────────────┐  │ │
│ │ Share Modal       │    │         │ │ SQLite Database      │  │ │
│ │ - Show stats      │    │         │ │ - shared_logs table  │  │ │
│ │ - Generate link   │    │         │ │ - ID, shareId, data  │  │ │
│ │ - Copy URL        │    │         │ │ - Timestamp          │  │ │
│ └───────────────────┘    │         │ └──────────────────────┘  │ │
│                          │         │                              │
│ ┌───────────────────┐    │         └──────────────────────────────┘
│ │ Shared View       │    │
│ │ - DPS charts      │    │         Port: 3001
│ │ - Statistics      │    │         URL: http://localhost:3001
│ │ - Skill breakdown │    │
│ │ - Damage-by-target│   │
│ └───────────────────┘    │
│                          │
│ Port: 5173               │
│ URL: http://localhost... │
└─────────────────────────┘
```

## User Journey

### Step 1: Upload & Analyze
```
┌─────────────────────────────────────────┐
│ 1. Open DPS Meter                       │
│    http://localhost:5173                │
├─────────────────────────────────────────┤
│ 2. Upload Combat Log File               │
│    └─ Drag & drop or click to browse    │
├─────────────────────────────────────────┤
│ 3. View Statistics                      │
│    ├─ DPS Over Time                     │
│    ├─ Player Stats Table                │
│    ├─ Damage by Skill                   │
│    ├─ Damage by Target                  │
│    └─ Skill Breakdown                   │
└─────────────────────────────────────────┘
```

### Step 2: Share Results
```
┌──────────────────────────────────────────┐
│ 4. Click "🔗 Share Results" Button       │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │      Share Your DPS Results          │ │
│ ├──────────────────────────────────────┤ │
│ │ Share your combat log results with   │ │
│ │ others using a unique link.          │ │
│ │                                      │ │
│ │ Stats Preview:                       │ │
│ │ ┌────────────────────────────────┐   │ │
│ │ │ Player: Livingg               │   │ │
│ │ │ Total Damage: 50,000          │   │ │
│ │ │ DPS: 100.50                   │   │ │
│ │ │ Duration: 497s                │   │ │
│ │ └────────────────────────────────┘   │ │
│ │                                      │ │
│ │ [Generate Share Link]                │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

### Step 3: Get & Share Link
```
┌──────────────────────────────────────────┐
│ 5. Share Link Generated!                 │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │                ✓                      │ │
│ │ Your results have been shared!       │ │
│ │                                      │ │
│ │ Share this link:                     │ │
│ │ ┌────────────────────────────────┐   │ │
│ │ │http://localhost:5173/share/... │   │ │
│ │ └────────────────────────────────┘   │ │
│ │                          [Copy]       │ │
│ │                                      │ │
│ │ Share with: Discord, Email, etc.     │ │
│ │                                      │ │
│ │              [Done]                  │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

### Step 4: Others View Results
```
┌──────────────────────────────────────────┐
│ 6. Friend Opens Shared Link              │
│    http://localhost:5173/share/a1b2c3d4  │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │        Combat Log Results            │ │
│ │                                      │ │
│ │ Player: Livingg                      │ │
│ │ Shared: 2024-12-06 10:30:00         │ │
│ │                                      │ │
│ │ [Total Damage] [DPS] [Duration]      │ │
│ │    50,000      100.50    497s        │ │
│ │                                      │ │
│ │ 📊 DPS Over Time (Chart)             │ │
│ │ 📊 Player Statistics (Table)         │ │
│ │ 📊 Damage by Skill (Chart)           │ │
│ │ 📊 Damage by Target (Table)          │ │
│ └──────────────────────────────────────┘ │
└──────────────────────────────────────────┘
```

## Data Flow

```
UPLOADING & SHARING
═════════════════════════════════════════════════════════════════

[User Device]
    ↓
    ├─ Select log file
    ↓
[Frontend - React]
    ├─ Parse log file
    ├─ Calculate statistics
    ├─ Display in UI
    ├─ Click "Share Results"
    ↓
[ShareModal Component]
    ├─ Show stats preview
    ├─ User confirms
    ↓
[API Client - api.ts]
    ├─ POST /api/share
    ├─ Send: playerName, totalDamage, dps, duration, logData
    ↓
[Network - HTTP]
    ↓
[Backend - Express Server]
    ├─ Receive request
    ├─ Generate shareId (uuid)
    ├─ Validate data
    ↓
[Database - SQLite]
    ├─ Insert row:
    │  ├─ shareId: 'a1b2c3d4'
    │  ├─ playerName: 'Livingg'
    │  ├─ totalDamage: 50000
    │  ├─ damagePerSecond: 100.50
    │  ├─ duration: 497
    │  ├─ logData: JSON string
    │  └─ createdAt: timestamp
    ↓
[Backend - API]
    ├─ Return shareId
    └─ Return shareUrl
    ↓
[Network - HTTP]
    ↓
[Frontend - ShareModal]
    ├─ Display URL
    ├─ Show copy button
    ├─ User copies URL
    ↓
[User Device]
    └─ Share URL via Discord/Email/etc


VIEWING SHARED RESULTS
═════════════════════════════════════════════════════════════════

[Friend's Device]
    ↓
    ├─ Click shared link
    └─ URL: /share/a1b2c3d4
    ↓
[Frontend - React Router]
    ├─ Route to /share/:shareId
    ├─ Mount SharedView component
    ↓
[SharedView Component]
    ├─ Extract shareId from URL
    ├─ Call api.retrieveSharedLog(shareId)
    ↓
[API Client - api.ts]
    ├─ GET /api/share/a1b2c3d4
    ↓
[Network - HTTP]
    ↓
[Backend - Express Server]
    ├─ Receive GET request
    ├─ Extract shareId
    ↓
[Database - SQLite]
    ├─ Query: SELECT * WHERE shareId = 'a1b2c3d4'
    ├─ Return row
    ↓
[Backend - API]
    ├─ Format response
    ├─ Return data
    ↓
[Network - HTTP]
    ↓
[Frontend - SharedView]
    ├─ Parse response
    ├─ Render DPS charts
    ├─ Render stats tables
    ├─ Render breakdowns
    ↓
[Browser]
    └─ Display full combat analysis
```

## Component Interaction

```
┌─────────────────────────────────────────────────────────────┐
│ App.tsx (Router Setup)                                       │
├─────────────────────────────────────────────────────────────┤
│ ├─ Route: / → DPSMeter component                            │
│ └─ Route: /share/:shareId → SharePage → SharedView          │
└─────────────────────────────────────────────────────────────┘
              │
              ├──────────────────────┬─────────────────────┐
              ↓                      ↓                     ↓
    ┌─────────────────┐  ┌──────────────────┐  ┌──────────────┐
    │ DPSMeter        │  │ SharePage        │  │ SharedView   │
    │ (Main Page)     │  │ (Route Handler)  │  │ (View Page)  │
    ├─────────────────┤  ├──────────────────┤  ├──────────────┤
    │ ├─ FileUpload   │  │ ├─ Extract ID    │  │ ├─ Fetch API │
    │ ├─ Charts       │  │ ├─ Create View   │  │ ├─ Parse data│
    │ ├─ Stats        │  │ └─ Pass shareId  │  │ ├─ DPSChart  │
    │ └─ ShareButton──┼──┤ (Props passing)  │  │ ├─ StatsTable│
    │    ↓ onClick    │  │                  │  │ ├─ SkillChart│
    │    ShareModal   │  │                  │  │ └─ DamageBy- │
    │    opens        │  │                  │  │   Target     │
    └─────────────────┘  └──────────────────┘  └──────────────┘
         │                                            ↑
         │                                            │
         └────────────────────────────────────────────┘
         Generate & Share Link      Fetch & Display
         (Backend API Call)         (Backend API Call)
```

## Technology Stack Visual

```
┌──────────────────────────────────────────────────────────────┐
│                     FRONTEND (Port 5173)                      │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────┐     ┌────────────────┐    ┌───────────────┐ │
│  │ React 19    │────▶│ React Router   │───▶│ TypeScript    │ │
│  │ (UI Layer)  │     │ (Routing)      │    │ (Type Safety) │ │
│  └─────────────┘     └────────────────┘    └───────────────┘ │
│                            │                                  │
│                            ↓                                  │
│  ┌──────────────┐    ┌────────────────┐    ┌──────────────┐  │
│  │ ShareModal   │    │ SharedView     │    │ DPSMeter     │  │
│  │ Components   │    │ Components     │    │ Components   │  │
│  └──────────────┘    └────────────────┘    └──────────────┘  │
│         │                   │                      │          │
│         └───────────────────┼──────────────────────┘          │
│                             ↓                                  │
│              ┌──────────────────────────┐                      │
│              │ api.ts (HTTP Client)     │                      │
│              │ • shareLog()             │                      │
│              │ • retrieveSharedLog()    │                      │
│              └──────────────────────────┘                      │
│                             │                                  │
└─────────────────────────────┼──────────────────────────────────┘
                              │ HTTP REST API
                              │ JSON
                              ↓
┌──────────────────────────────────────────────────────────────┐
│                   BACKEND (Port 3001)                         │
├──────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌────────────────┐       ┌───────────────────┐              │
│  │ Express.js     │──────▶│ TypeScript        │              │
│  │ (Web Server)   │       │ (Type Safety)     │              │
│  └────────────────┘       └───────────────────┘              │
│         │                                                     │
│         ├─ POST /api/share                                    │
│         ├─ GET /api/share/:shareId                           │
│         └─ GET /health                                        │
│                                                               │
│  ┌────────────────────────────────────────┐                 │
│  │ Database Layer (db.ts)                 │                 │
│  │ • saveLog()                            │                 │
│  │ • getLogByShareId()                    │                 │
│  │ • getAllLogs()                         │                 │
│  └────────────────────────────────────────┘                 │
│         │                                                     │
│         ↓                                                     │
│  ┌────────────────────────────────────────┐                 │
│  │ SQLite Database (dps_shares.db)        │                 │
│  │                                        │                 │
│  │ shared_logs table:                     │                 │
│  │ • id (INT, PRIMARY KEY)                │                 │
│  │ • shareId (TEXT, UNIQUE)               │                 │
│  │ • playerName (TEXT)                    │                 │
│  │ • totalDamage (INT)                    │                 │
│  │ • damagePerSecond (REAL)               │                 │
│  │ • duration (INT)                       │                 │
│  │ • timestamp (INT)                      │                 │
│  │ • logData (TEXT - JSON)                │                 │
│  │ • createdAt (DATETIME)                 │                 │
│  └────────────────────────────────────────┘                 │
│                                                               │
└──────────────────────────────────────────────────────────────┘
```

## Share Link Format

```
Base URL: http://localhost:5173
                   ↓
        Share Route: /share/:shareId
                   ↓
Complete URL: http://localhost:5173/share/a1b2c3d4
                                              └─ 8-char unique ID
                                              generated by UUID lib
```

## Error Handling Flow

```
User Action
    ↓
[Try to share]
    ├─ Success ────→ Show URL
    └─ Error ──────→ Show error message
                      ├─ Network error
                      ├─ Server error
                      └─ Invalid data


[Viewing shared log]
    ├─ Loading ────→ Show spinner
    ├─ Success ────→ Display results
    └─ Error ──────→ Show error message
                      ├─ Share not found
                      ├─ Server error
                      └─ Network error
```

---

**Visual guide complete!** 📊

For more details, see SHARING_FEATURE.md or SHARING_QUICKSTART.md
